# 14.zettel-writer-connect 운영가이드

Version: v1.2 / 2026-05-20

이 문서는 대표님 환경에서 AI 원고실 Obsidian 플러그인을 운영·수정·검증할 때의 기준 문서다. 핵심 원칙은 간단하다.

- 14번 폴더는 AI 원고실을 안전하게 분석·개조하는 별도 작업장이다.
- 13번 Zettel Connect는 기존 지식관리 엔진의 원본이므로 함부로 섞지 않는다.
- 실제 Obsidian vault 쓰기, deploy, restart/reload는 대표님이 명시 요청할 때만 한다.
- 완료 주장은 테스트·빌드·Git/PR 동기화까지 확인한 뒤에만 한다.

---

## 1. 현재 판단: 왜 14번 폴더인가

유료로 받은 Obsidian 글쓰기 플러그인은 독립된 제품 성격이 있다. 그래서 기존 13번 Zettel Connect에 바로 합치지 않고, 14번 프로젝트에서 분석·개조·검증한다.

| 항목 | 판단 |
|---|---|
| 13.zettel-connect | 대표님 지식창고/Zettelkasten 연결 엔진의 기존 원본. 안정성이 우선이다. |
| 14.zettel-writer-connect | AI 원고실, 글쓰기 작업실, Zettel 구조노트 handoff를 다루는 별도 작업장. |
| 연결 방식 | 직접 내부 API 결합이 아니라 `_index/writing-handoff.json` 파일 handoff 방식. |
| 향후 통합 | W4처럼 명시 태스크가 있을 때만 13번에 “원고실로 보내기” 버튼을 추가한다. |

금지:

```text
/Users/dongchanyoon/Documents/Work/Projects/13.zettel-connect/**
```

위 경로는 별도 허가 없이 수정하지 않는다.

---

## 2. 프로젝트 위치와 플러그인 ID

| 항목 | 값 |
|---|---|
| 소스 루트 | `/Users/dongchanyoon/Documents/Work/Projects/14.zettel-writer-connect` |
| AI 원고실 plugin id | `ai-manuscript-studio` |
| Zettel Connect bundle plugin id | `zettel-connect` |
| 대표님 주 vault | `/Users/dongchanyoon/Library/CloudStorage/OneDrive-개인/지식창고` |
| 기본 공유 설치 루트 | `~/.local/obsidian-plugins/<plugin-id>/` |

주의: deploy 스크립트의 기본 `OBSIDIAN_VAULT_PLUGINS_DIR`는 원 개발자 `futurewave` 경로다. 대표님 vault에 배포할 때는 반드시 환경 변수를 지정한다.

---

## 3. 패키지 구조

```text
packages/core/
  AI 원고실 공통 로직: project.json, binder, sourceNotes, AI context composer.

packages/obsidian-plugin/
  실제 AI 원고실 Obsidian 플러그인.
  manifest id: ai-manuscript-studio
  main.ts에 Obsidian command가 등록된다.

packages/zettel-connect/
  이 repo 안에 들어 있는 Zettel Connect bundle 사본.
  13.zettel-connect 원본을 대체하지 않는다.

apps/desktop/
  예전 Tauri 앱 잔존 코드. 현재 운영 대상 아님.
```

---

## 4. 대표님 글쓰기 워크플로우

목표는 `/structure` 스킬로 만든 구조노트를 AI 원고실에서 곧바로 원고 프로젝트로 이어 쓰는 것이다.

```text
1. 지식창고에서 영구노트/문헌노트 축적
   ↓
2. /structure 스킬로 구조노트 작성
   예: 3.Structure/투자전략-보고서.md
   ↓
3. Zettel Connect 또는 Hermes가 handoff 파일 생성
   _index/writing-handoff.json
   ↓
4. AI 원고실 command 실행
   원고실 handoff JSON 가져오기
   ↓
5. 4.Writing/<slug>/ 프로젝트 생성
   project.json / binder.json / planning.md
   ↓
6. AI 원고실에서 장면별 작성·수정·코칭
```

현재 구현된 command:

| 단계 | command id | Obsidian 표시명 | 상태 |
|---|---|---|---|
| W1 | `import-active-structure-note` | 현재 구조노트를 원고 프로젝트로 가져오기 | 구현 완료 |
| W3 | `import-writing-handoff-json` | 원고실 handoff JSON 가져오기 | 구현 완료 |

---

## 5. `_index/writing-handoff.json` 계약

AI 원고실은 vault 안의 정확히 이 파일을 읽는다.

```text
_index/writing-handoff.json
```

예시:

```json
{
  "version": 1,
  "mode": "new-structure-to-writing",
  "structureNote": {
    "path": "3.Structure/투자전략-보고서.md",
    "title": "투자전략 보고서",
    "id": "S-001",
    "claim": "핵심 주장은 여기에 적는다"
  },
  "picked": [
    { "path": "2.Permanent/시장-사이클.md", "id": "P-001" },
    { "path": "2.Permanent/리스크-프리미엄.md", "id": "P-002" }
  ],
  "project": {
    "title": "투자전략 보고서 초안",
    "genre": "investment-strategy-memo",
    "wordGoal": 5000,
    "status": "planning"
  },
  "targetWritingFolder": "4.Writing"
}
```

필수/검증 규칙:

- `structureNote.path`는 필수다.
- 모든 path는 vault-relative POSIX 경로여야 한다.
- `/Users/...` 같은 절대 경로는 reject된다.
- `../` parent traversal은 reject된다.
- `picked`는 없을 수 있다.
- `project`, `targetWritingFolder`도 선택 필드다.

생성되는 `project.json.sourceNotes`는 다음 순서다.

```json
{
  "sourceNotes": [
    "3.Structure/투자전략-보고서.md",
    "2.Permanent/시장-사이클.md",
    "2.Permanent/리스크-프리미엄.md"
  ]
}
```

첫 번째는 구조노트, 그 뒤는 picked 영구노트다. 중복은 순서를 보존하면서 제거한다.

---

## 6. 빌드·테스트 명령

소스 루트:

```bash
cd /Users/dongchanyoon/Documents/Work/Projects/14.zettel-writer-connect
```

의존성:

```bash
pnpm install
```

AI 원고실 targeted 테스트:

```bash
pnpm --filter @ai-manuscript-studio/obsidian-plugin test -- \
  --runTestsByPath tests/structureBridge.test.ts tests/mainStructureBridgeCommand.test.ts
```

현재 기준:

```text
29/29 PASS
```

AI 원고실 전체 테스트:

```bash
pnpm --filter @ai-manuscript-studio/obsidian-plugin test
```

현재 기준:

```text
268/268 PASS
```

core 테스트:

```bash
pnpm --filter @ai-manuscript-studio/core test
```

AI 원고실 빌드:

```bash
pnpm --filter @ai-manuscript-studio/obsidian-plugin build
node --check packages/obsidian-plugin/main.js
```

전체 플러그인 빌드:

```bash
pnpm build:plugins
```

---

## 7. 배포 절차

배포는 실제 Obsidian 런타임에 영향을 준다. 대표님이 “배포해”, “설치해”, “옵시디언에 반영해”라고 명시할 때만 실행한다.

대표님 vault에 AI 원고실만 배포:

```bash
cd /Users/dongchanyoon/Documents/Work/Projects/14.zettel-writer-connect

OBSIDIAN_VAULT_PLUGINS_DIR="/Users/dongchanyoon/Library/CloudStorage/OneDrive-개인/지식창고/.obsidian/plugins" \
  pnpm deploy:ai-manuscript
```

두 플러그인 모두 배포:

```bash
OBSIDIAN_VAULT_PLUGINS_DIR="/Users/dongchanyoon/Library/CloudStorage/OneDrive-개인/지식창고/.obsidian/plugins" \
  pnpm deploy
```

배포 후 Obsidian에서 필요:

```text
Cmd+P → Reload app without saving 또는 개발자용 다시 로드
또는 설정 → 커뮤니티 플러그인 → 해당 플러그인 off/on
```

단순 reload만 요청받은 경우에는 build/deploy를 실행하지 않는다. 먼저 현재 설치·활성 상태를 읽고, 앱만 새로고침한다.

```bash
VAULT="/Users/dongchanyoon/Library/CloudStorage/OneDrive-개인/지식창고"

# Obsidian 실행 여부
pgrep -fl Obsidian || true

# 플러그인 설치 경로와 symlink 확인
python3 - <<'PY'
from pathlib import Path
vault=Path('/Users/dongchanyoon/Library/CloudStorage/OneDrive-개인/지식창고')
for pid in ['ai-manuscript-studio','zettel-connect','obsidian42-brat']:
    p=vault/'.obsidian/plugins'/pid
    print(pid, 'exists=', p.exists(), 'is_symlink=', p.is_symlink(), 'path=', p)
    if p.is_symlink():
        print('  ->', p.resolve())
PY

# 플러그인 활성화 여부 확인
python3 - <<'PY'
from pathlib import Path
import json
p=Path('/Users/dongchanyoon/Library/CloudStorage/OneDrive-개인/지식창고/.obsidian/community-plugins.json')
data=json.loads(p.read_text())
for pid in ['ai-manuscript-studio','zettel-connect']:
    print(pid, pid in data)
PY
```

가장 확실한 reload는 정상 종료 후 재실행이다. 강제 종료가 아니라 AppleScript quit를 사용한다.

```bash
BEFORE=$(pgrep -x Obsidian || true)
printf 'before_pid=%s\n' "$BEFORE"

osascript -e 'tell application "Obsidian" to quit' || true
for i in $(seq 1 20); do
  if ! pgrep -x Obsidian >/dev/null 2>&1; then
    echo "quit_confirmed_after=${i}s"
    break
  fi
  sleep 1
done

open -a Obsidian
for i in $(seq 1 20); do
  if pgrep -x Obsidian >/dev/null 2>&1; then
    echo "reopen_confirmed_after=${i}s"
    break
  fi
  sleep 1
done

AFTER=$(pgrep -x Obsidian || true)
printf 'after_pid=%s\n' "$AFTER"
```

배포 후 확인:

```bash
ls -l "/Users/dongchanyoon/Library/CloudStorage/OneDrive-개인/지식창고/.obsidian/plugins/ai-manuscript-studio"
```

완료 주장 전에는 source bundle과 vault 설치 bundle의 `main.js`, `manifest.json`, `styles.css` 존재 및 SHA를 비교한다. reload만 수행한 경우에는 “deploy는 하지 않았다”고 명시한다.

---

## 8. 완료 전 검증 체크리스트

문서만 고친 경우:

```bash
git diff --check -- 운영가이드.md
test -s 운영가이드.md
```

AI 원고실 코드/테스트를 고친 경우:

```bash
pnpm --filter @ai-manuscript-studio/obsidian-plugin test -- \
  --runTestsByPath tests/structureBridge.test.ts tests/mainStructureBridgeCommand.test.ts
pnpm --filter @ai-manuscript-studio/obsidian-plugin test
pnpm --filter @ai-manuscript-studio/obsidian-plugin build
node --check packages/obsidian-plugin/main.js
git diff --check
```

금지 경로 확인:

```bash
git status --short
```

다음이 의도 없이 보이면 중단하고 확인한다.

```text
/Users/dongchanyoon/Documents/Work/Projects/13.zettel-connect/**
지식창고/.obsidian/**
지식창고/_index/**
지식창고/2.Permanent/**
지식창고/3.Structure/**
지식창고/4.Writing/**
```

---

## 9. Git / PR 운영

현재 GitHub 권한 구조:

| remote | 의미 | 권한 |
|---|---|---|
| origin | `vibelabs-web/zettel-writer-connect` upstream | 현재 계정은 push 불가 |
| fork | `dyoon98-creator/zettel-writer-connect` | push 가능 |

퍼블리시 브랜치:

```text
feat/ai-manuscript-studio-workflow-20260519
```

PR:

```text
PR #2
https://github.com/vibelabs-web/zettel-writer-connect/pull/2
```

커밋 후 push:

```bash
git push fork HEAD:feat/ai-manuscript-studio-workflow-20260519
```

동기화 확인:

```bash
git fetch fork feat/ai-manuscript-studio-workflow-20260519 --quiet
git rev-list --left-right --count HEAD...fork/feat/ai-manuscript-studio-workflow-20260519
# 기대: 0 0

gh pr view 2 --repo vibelabs-web/zettel-writer-connect --json headRefOid --jq .headRefOid
git rev-parse HEAD
# 두 SHA가 같아야 한다.
```

`git status`가 다음처럼 보여도 fork/PR이 맞으면 정상이다.

```text
## main...origin/main [ahead N]
```

이유: origin은 upstream이고 현재 계정에 push 권한이 없기 때문이다.

---

## 10. 자주 나는 문제

| 증상 | 판단 / 조치 |
|---|---|
| `_index/writing-handoff.json` 없음 | 정상 no-op. handoff 생성 전 상태다. |
| `vault-relative path` 오류 | handoff JSON 안에 절대경로 또는 `..`가 들어갔다. vault-relative로 고친다. |
| command가 Obsidian에서 안 보임 | deploy 후 Obsidian reload/off-on이 안 됐을 가능성. |
| AI CLI가 작동하지 않음 | Codex/Claude CLI 경로와 로그인 상태를 확인한다. API key를 repo에 넣지 않는다. |
| source 수정했는데 UI가 그대로임 | build/deploy/reload가 안 된 상태다. source 변경만으로 live plugin은 바뀌지 않는다. |
| `styles.css`를 고쳐도 되나 | 직접 고치지 않는다. source CSS를 고치고 build한다. |
| `packages/zettel-connect`를 고쳐도 되나 | 원칙적으로 금지. 13번 원본과의 동기화 계획이 있을 때만 한다. |

---

## 11. 현재 완료된 작업 기준

| ID | 상태 | 의미 |
|---|---|---|
| W1 | 완료 | 활성 구조노트에서 AI 원고실 프로젝트 생성 |
| W2 | 완료 | `project.json.sourceNotes`를 AI context에 실제 주입 |
| W3 | 완료 | `_index/writing-handoff.json`에서 AI 원고실 프로젝트 생성 |
| W3A | 완료 | 절대경로/parent traversal reject regression test |
| W4 | 미실행 | 13.zettel-connect에 “원고실로 보내기” 버튼 추가. 별도 허가 필요 |

W4를 할 때만 13번 repo를 건드린다. 그 전까지는 14번에서 AI 원고실을 안정화한다.

---

## 12. 운영 원칙 한 줄 요약

이 프로젝트는 “기존 Zettelkasten을 망가뜨리지 않고, 구조노트를 원고 프로젝트로 안전하게 넘기는 글쓰기 작업실”이다. 13번은 지식 연결 엔진, 14번은 원고 작성 엔진으로 분리해서 운영한다.
